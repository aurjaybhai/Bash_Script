#!/bin/bash

for (( ; ; )); do # for (( initialization; condition; increment/decrement ))
    # No initialization (left empty)
    # No condition (empty means "always true")
    # No increment/decrement (left empty)
    echo "Purushotam"
    sleep 1s
done
